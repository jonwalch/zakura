## Removed

- Removed the unused `rpc.tls` configuration and RPC TLS listener left behind
  after zcashd compatibility sync moved to the P2P sidecar
  ([#514](https://github.com/zakura-core/zakura/pull/514)).
