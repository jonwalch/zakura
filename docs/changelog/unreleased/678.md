## Changed

- Updated the default Mainnet native bootstrap peers to the current public
  nodes
  ([#678](https://github.com/zakura-core/zakura/pull/678)).

## Fixed

- Count authenticated Zakura connections in `/healthy` and `/ready`, so
  `p2p_stack = "zakura"` nodes are not stuck on `insufficient peers`
  ([#678](https://github.com/zakura-core/zakura/pull/678)).
- Persist a controller halt even when run artifacts cannot be written, and do
  not page a node-down for leftover `syncing` state after the controller
  process itself has died
  ([#678](https://github.com/zakura-core/zakura/pull/678)).
